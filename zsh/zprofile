#!/usr/bin/env zsh

if_installed() { command -v "$1" > /dev/null; }

if_installed vim && export EDITOR="vim"
if_installed nvim && export EDITOR="nvim"

export GOPATH="$HOME/.go"

unset if_installed

# https://github.com/folke/tokyonight.nvim/blob/main/extras/fzf/tokyonight_night.sh
export FZF_DEFAULT_OPTS="$FZF_DEFAULT_OPTS \
  --highlight-line \
  --info=inline-right \
  --ansi \
  --layout=reverse \
  --border=none
"

eval "$(/opt/homebrew/bin/brew shellenv)"
